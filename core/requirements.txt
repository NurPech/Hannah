faster-whisper>=1.0.0
av>=11          # OGG/Opus-Transkodierung (main.py::_handle_voice), ersetzt ffmpeg-Subprocess (#244)
paho-mqtt>=2.0.0
PyYAML>=6.0
requests>=2.31.0
numpy>=1.24.0,<3   # Obergrenze gegen unbeabsichtigten Major-Sprung (#164)
piper-tts>=1.2.0    # TTS-Synthese (optional, deaktivierbar via config)
miniaudio>=1.58     # Audio-Dekodierung (MP3, WAV, OGG, FLAC) für Bestätigungstöne
grpcio>=1.83.0      # gRPC-Laufzeit (Server + Client)
protobuf>=7.35.0    # hannah-proto's gencode braucht mind. diese Runtime-Version
hannah-proto>=3.8.0 # Proto schema — published package (#60)
werkzeug>=3.1.0     # Password-Hashing (generate_password_hash)
dialectorm-m1kad0>=0.1.2 # Mini-ORM (import name: pyorm), published package (#132)
mysql-connector-python>=8.0 # MySQL-Treiber für pyorm.Database.mysql() — Activity-Log-DB (#220)
