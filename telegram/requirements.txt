# Telegram bot
python-telegram-bot>=20.0

# gRPC
grpcio>=1.84.0     # hannah-proto>=4.2.1's generated grpc code requires this floor
protobuf>=7.35.0

# Proto schema — published package instead of the git-submodule/local-codegen
# pattern (#60); pins the runtime proto/gRPC-version coupling that used to be
# handled by matching grpcio-tools' version locally. Core enforces an exact
# x-proto-version match, so this MUST move in lockstep with core/requirements.txt.
# >=4.2.1 fixes a grpcio-tools floor mismatch in the published gencode
hannah-proto>=4.3.0

# Log shipping to the Hannah log collector (#341)
hannah-logging>=0.3.1  # 0.3.1: stable instance name in containers (#356)

# Config
pyyaml>=6.0

# Optional: for development / testing
# pytest>=8.0
